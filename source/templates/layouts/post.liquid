---
layout: "base"
---

<article class="prose">
	<header>
		<h1>{{ title }}</h1>
		{% if subtitle %}<p>{{ subtitle }}</p>{% endif %}
		<time>{{ page.date | formatDate }}</time>
	</header>

	{{ content }}
</article>
