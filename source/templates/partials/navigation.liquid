<nav class="site-nav">
	<ul role="list">
		{% for item in site.navigation %}
		<li>
			<a href="{{ item.href }}" {% if page.url == item.href %} aria-current="page"{% endif %}>{{ item.title }}</a>
		</li>
		{% endfor %}
	</ul>
</nav>