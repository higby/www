---
title: "hypertext garden"
subtitle: "<a href='/feed.xml'>rss</a>"
permalink: "/garden/"
---

<ol class="lattice" role="list">
	{% for item in collections.garden reversed %}
	<li>
		<time>{{ item.date | formatDate }}</time>
		<a href="{{ item.url | default: item.data.externalURL }}">{{ item.data.title }}</a>
	</li>
	{% endfor %}
</ol>
